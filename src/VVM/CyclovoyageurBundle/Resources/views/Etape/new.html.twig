{% extends '::base.html.twig' %}

{% block title %}Ajouter une étape - {{ parent() }}{% endblock title %}

{% block body -%}
    <h1>Ajouter une étape</h1>

    {{ form(form) }}

    <div class="btn-margin">
        <a class="btn btn-danger" href="{{ path('etape') }}">Retour</a>
    </div>

{% endblock %}
