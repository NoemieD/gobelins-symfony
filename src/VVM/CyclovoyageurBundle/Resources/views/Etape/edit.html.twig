{% extends '::base.html.twig' %}

{% block title %}Éditer une étape - {{ parent() }}{% endblock title %}

{% block body -%}
    <h1>Éditer une étape</h1>

    {{ form(edit_form) }}


        <a class="btn btn-warning" href="{{ path('etape') }}">
            Retour
        </a>

   {{ form(delete_form) }}

{% endblock %}
