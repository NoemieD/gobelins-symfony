{% extends '::base.html.twig' %}

{% block title %}Éditer un arrêt - {{ parent() }}{% endblock title %}

{% block body -%}
    <h1>Éditer un arrêt</h1>

    {{ form(edit_form) }}


        <a href="{{ path('arret') }}">
            Retour
        </a>

    {{ form(delete_form) }}

{% endblock %}
