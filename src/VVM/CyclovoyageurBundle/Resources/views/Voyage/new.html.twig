{% extends '::base.html.twig' %}

{% block title %}Ajouter un voyage - {{ parent() }}{% endblock title %}

{% block body -%}
    <h1>Voyage creation</h1>

    {{ form(form) }}

        <a class="btn btn-warning" href="{{ path('voyage') }}">
            Back to the list
        </a>

{% endblock %}
