{% extends '::base.html.twig' %}

{% block title %}Ajouter un voyage - {{ parent() }}{% endblock title %}

{% block body -%}
    <h1>Ajouter un voyage</h1>

    {{ form(form) }}

    <div class=btn-margin">
        <a class="btn btn-primary" href="{{ path('voyage') }}">Retour</a>
    </div>

{% endblock %}
